# TRUST_CASE_POST — repo-only publish notes (never paste into dev.to)

- COVER IMAGE (required): three horizontal bands — FREE (teal) / ANNOUNCED
  (cyan) / GATED (amber card with an approve button) — dark palette, same
  style family as the wire-stub cover. No unscrubbed app screenshots.
- PUBLISH GATE: operator's explicit go + the posting-cadence window (the
  wire-stub story must finish its rollout first). Re-verify every claim
  against disk on publish day. Frontmatter pasted to dev.to must be ONLY
  title/published/tags/cover_image.
